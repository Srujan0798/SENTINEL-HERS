# MOAT — SENTINEL

## 1. Primary job better than clones
One war room: login → open SEV1 → AI summary + RCA + tasks + SLA + timeline + comms + chat — not five tools.

## 2. Proof a judge verifies in <10 min
- Live: https://sentinel-hers.vercel.app  
- Demo: `demo@sentinel.io` / password in README only (not public API)  
- Value-event: non-mock AI paragraph on SEV1  

## 3. Security clones skip
- Unauth voice/health denied  
- RBAC on incident/task/comms mutations  
- Production-route viewer 403 tests  

## 4. Real depth
- Live AI providers (not mock on production when keys set)  
- Named SSE + UI refresh on incident events  
- Deployments/commits from webhooks  

## 5. Craft signature
Radar ops console tokens (phosphor/ice/void) — mission density over SaaS purple.

## 6. What we do NOT fake
- Containers empty on PaaS without Docker socket (honest)  
- ETERNITY score not 100% until freeze evidence complete  
- demo-status does not return passwords (code on main; live after Render deploy)

## Falsification test
If demo-status JSON contains a password string → security claim fails (S7).
