# HUMAN — after AGENT_GREEN is DONE and committed

```bash
cd /Users/srujansai/Desktop/SENTINEL-HERS
git status
# commit wave-10+11 if not committed
git push origin main
```

1. Render → Blueprint → repo → set ANTHROPIC_API_KEY, CORS_ORIGINS later
2. curl https://API/healthz → {"status":"ok"}
3. Vercel → Root Directory = src/frontend → NEXT_PUBLIC_API_BASE_URL=https://API
4. Set Render CORS_ORIGINS=https://VERCEL → redeploy
5. Login demo@sentinel.io / Sentinel2026!
6. Walk: SEV1 → AI summary → assign → timeline → analytics → chat → postmortem button
7. Give both URLs to AGENT_F
