# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# Auth
pyjwt>=2.8.0
bcrypt>=4.1.0
passlib[bcrypt]>=1.7.4

# Caching / realtime
redis>=5.0.0
aiohttp>=3.9.0

# Observability
prometheus-client>=0.20.0

# AI layer (all optional — falls back to mock if key absent)
anthropic>=0.40.0
google-generativeai>=0.8.0
openai>=1.40.0

# ML
scikit-learn>=1.4.0
joblib>=1.4.0
numpy>=1.26.0

# Integrations (optional — graceful fallback if not installed)
docker>=7.0.0
kubernetes>=29.0.0

# Utilities
pydantic>=2.0.0
pydantic[email]>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
requests>=2.31.0

# Test
pytest>=8
pytest-asyncio
slowapi
